<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModel="clr-namespace:AwfulRedux.Mobile.ViewModel;assembly=AwfulRedux.Mobile"
             x:Class="AwfulRedux.Mobile.Views.MainForumsPage"
             Title="Main Forums">

  <ContentPage.BindingContext>
    <viewModel:MainForumsPageViewModel/>
  </ContentPage.BindingContext>
  
 <ContentPage.Content>
   <AbsoluteLayout 
     HorizontalOptions="FillAndExpand"
     VerticalOptions="FillAndExpand">
     <StackLayout Orientation="Vertical" Padding="0,20,0,0" AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,0,1,1" Spacing="0">
       <ListView  x:Name="listView" IsGroupingEnabled="True" GroupDisplayBinding="{Binding Name}" HeightRequest="200" ItemsSource="{Binding ForumCategories}" HasUnevenRows="true">
         <ListView.ItemTemplate>
           <DataTemplate>
             <TextCell Text="{Binding Name}"/>
           </DataTemplate>
         </ListView.ItemTemplate>
       </ListView>
     </StackLayout>
   </AbsoluteLayout>
 </ContentPage.Content>
</ContentPage>