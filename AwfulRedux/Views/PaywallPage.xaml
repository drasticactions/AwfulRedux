<Page
    x:Class="AwfulRedux.Views.PaywallPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AwfulRedux.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock FontWeight="Bold" TextAlignment="Center" Style="{ThemeResource HeaderTextBlockStyle}" Text="BUY A FORUMS ACCOUNT"/>
            <MediaElement Grid.Row="1" Margin="5" Source="/Assets/sa-clip.mp4" AutoPlay="True" AreTransportControlsEnabled="True"/>
        </Grid>
    </Grid>
</Page>
